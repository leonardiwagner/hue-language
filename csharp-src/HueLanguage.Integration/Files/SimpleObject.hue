class person
  text firstName = "joe"
  text lastName
  number age = 18

  walk
    print "walked"

  text getName
    return this.firstName + " " + this.lastName

  setAge(number age)
    this.age = age

  person setPerson(text firstName, text lastName, number age)
    this.firstName = firstName
    this.lastName = lastName
    this.age = age